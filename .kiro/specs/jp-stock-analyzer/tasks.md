# 実装計画: 日本株分析AIエージェント

## 概要

既存の Amplify Gen 2 + Strands Agents SDK アーキテクチャを活用し、日本株分析AIエージェント機能を段階的に実装する。エージェント側（Python）のツール・エージェント定義から着手し、フロントエンド（TypeScript）のUI・通信層を構築、最後に結合して動作確認する。

## タスク

- [x] 1. エージェント基盤セットアップとデータ型定義
  - [x] 1.1 フロントエンド型定義ファイルを作成する
    - `src/types/stock.ts` に `StockDataPoint`, `StockDataPayload` インターフェースを定義する
    - _要件: 10.1, 10.2_
  - [x] 1.2 エージェントディレクトリ構造を作成する
    - `agents/jp_stock_agent/` ディレクトリに `__init__.py`, `agent.py`, `tools.py`, `app.py` の空ファイルを作成する
    - `agents/pyproject.toml` の `packages.find.include` に `jp_stock_agent*` を追加する
    - `agents/requirements.txt` に `yfinance` を追加する
    - _要件: 9.1, 9.2_

- [x] 2. 銘柄検索・株価データ取得ツールの実装
  - [x] 2.1 `search_stock` ツールを実装する
    - `agents/jp_stock_agent/tools.py` に `@tool` デコレータ付きの `search_stock(query: str) -> str` を実装する
    - yfinance を使用して銘柄コード（4桁数字）または銘柄名から日本の上場企業を特定する
    - 一致する銘柄が見つからない場合はエラーメッセージを返す
    - 外部APIエラー時はエラー内容をログに記録し、エラーメッセージを返す
    - _要件: 1.2, 1.3, 1.4, 9.1, 9.2, 9.4, 9.5_

  - [ ]* 2.2 `search_stock` のプロパティテストを作成する
    - **Property 1: 銘柄検索の入力解決**
    - hypothesis を使用し、有効な銘柄コード/名に対して企業名と銘柄コードの両方を含む結果が返ることを検証する
    - **検証対象: 要件 1.2, 1.3**

  - [ ]* 2.3 `search_stock` の無効入力プロパティテストを作成する
    - **Property 2: 無効入力のエラー検出**
    - hypothesis を使用し、存在しない銘柄コードや意味のない文字列に対してエラー結果が返ることを検証する
    - **検証対象: 要件 1.4**

  - [x] 2.4 `get_stock_prices` ツールを実装する
    - `agents/jp_stock_agent/tools.py` に `@tool` デコレータ付きの `get_stock_prices(ticker_code: str) -> str` を実装する
    - yfinance で過去1年分の日次株価データ（始値、高値、安値、終値、出来高）を取得する
    - 5日、25日、75日、200日の単純移動平均線（SMA）を算出する（データ不足期間は null）
    - 結果を `StockDataPayload` 互換の JSON 文字列として返す（日付昇順）
    - 1年分のデータが取得できない場合は取得可能な期間で実行し、データ期間を明示する
    - _要件: 3.1, 3.2, 3.4, 10.1, 10.2, 9.1, 9.2, 9.4, 9.5_

  - [ ]* 2.5 `get_stock_prices` のプロパティテストを作成する
    - **Property 4: 株価データ構造の完全性**
    - hypothesis を使用し、有効な銘柄コードに対して出力JSONが必須フィールドを含み日付昇順であることを検証する
    - **検証対象: 要件 3.1, 10.1, 10.2**

  - [ ]* 2.6 移動平均線算出のプロパティテストを作成する
    - **Property 5: 移動平均線の算出正確性**
    - hypothesis を使用し、任意の終値配列と期間Nに対してSMAが数学的に正しいことを検証する
    - N未満のインデックスでは null であることも検証する
    - **検証対象: 要件 3.2**

- [x] 3. 財務指標・ニュース・同業種比較ツールの実装
  - [x] 3.1 `get_financial_metrics` ツールを実装する
    - `agents/jp_stock_agent/tools.py` に `@tool` デコレータ付きの `get_financial_metrics(ticker_code: str) -> str` を実装する
    - yfinance で PER、PBR、ROE、配当利回り、時価総額、売上高、営業利益、純利益、自己資本比率を取得する
    - 取得できない項目は null として含め、フィールド自体は必ず存在させる
    - 各指標の業種平均との比較評価コメントを生成する
    - _要件: 2.1, 2.2, 2.3, 2.4, 9.1, 9.2, 9.4, 9.5_

  - [ ]* 3.2 `get_financial_metrics` のプロパティテストを作成する
    - **Property 3: 財務指標の完全性**
    - hypothesis を使用し、有効な銘柄コードに対して必須フィールドがすべて存在することを検証する
    - **検証対象: 要件 2.1, 2.2**

  - [x] 3.3 `search_news` ツールを実装する
    - `agents/jp_stock_agent/tools.py` に `@tool` デコレータ付きの `search_news(company_name: str, ticker_code: str) -> str` を実装する
    - yfinance の `Ticker.news` で直近のIR・ニュース情報を取得する
    - `TAVILY_API_KEY` 環境変数が設定されている場合は Tavily API による拡張検索も実行する
    - 各情報の概要と株価への影響度（ポジティブ・ネガティブ・ニュートラル）を含める
    - 情報が取得できない場合はその旨を明示する
    - _要件: 5.1, 5.2, 5.3, 9.1, 9.2, 9.3, 9.4, 9.5_

  - [x] 3.4 `get_sector_peers` ツールを実装する
    - `agents/jp_stock_agent/tools.py` に `@tool` デコレータ付きの `get_sector_peers(ticker_code: str) -> str` を実装する
    - yfinance で同一業種の主要上場企業（3社以上）を特定する
    - 対象銘柄と同業種他社の PER、PBR、ROE、配当利回り、時価総額を比較する
    - 業界内での相対的な位置づけ（割安・割高、成長性、収益性）の評価コメントを含める
    - _要件: 6.1, 6.2, 6.3, 9.1, 9.2, 9.4, 9.5_

  - [ ]* 3.5 `get_sector_peers` のプロパティテストを作成する
    - **Property 6: 同業種比較データの完全性**
    - hypothesis を使用し、有効な銘柄コードに対して3社以上の比較データが返ることを検証する
    - **検証対象: 要件 6.1, 6.2**

- [x] 4. チェックポイント - エージェントツールの動作確認
  - すべてのテストが通ることを確認し、不明点があればユーザーに質問する。

- [x] 5. エージェント本体とスコアリングロジックの実装
  - [x] 5.1 スコアリングロジックを実装する
    - `agents/jp_stock_agent/tools.py` または `agents/jp_stock_agent/scoring.py` に総合スコア算出ロジックを実装する
    - ファンダメンタルズ（0〜30点）、テクニカル（0〜25点）、IR・ニュース（0〜20点）、同業種比較（0〜25点）の各カテゴリスコアを算出する
    - 総合スコア（0〜100点）= 各カテゴリスコアの合計
    - 総合スコアに基づく投資判断ラベル（強気・やや強気・中立・やや弱気・弱気）を生成する（スコアが高いほどポジティブ、単調性を保証）
    - _要件: 7.1, 7.2, 7.3, 7.4_

  - [ ]* 5.2 スコア整合性のプロパティテストを作成する
    - **Property 7: スコア整合性**
    - hypothesis を使用し、ランダムなカテゴリスコアに対して総合スコアが0〜100の範囲内かつ各カテゴリの合計と等しいことを検証する
    - **検証対象: 要件 7.1, 7.2**

  - [ ]* 5.3 スコアと判断ラベルのプロパティテストを作成する
    - **Property 8: スコアと判断ラベルの対応**
    - hypothesis を使用し、ランダムなスコアに対して判断ラベルの単調性を検証する
    - **検証対象: 要件 7.4**

  - [x] 5.4 エージェント定義を実装する
    - `agents/jp_stock_agent/agent.py` に `create_agent()` 関数を実装する
    - system_prompt に分析手順、出力フォーマット、JSON マーカー規約（`<!--STOCK_DATA_JSON-->...<!--/STOCK_DATA_JSON-->`）、免責事項表示の指示を含める
    - tools に `search_stock`, `get_stock_prices`, `get_financial_metrics`, `search_news`, `get_sector_peers` を登録する
    - 共通設定（`common.config`, `common.logging`）を使用する
    - _要件: 9.1, 9.2, 7.5_

  - [x] 5.5 AgentCore Runtime エントリーポイントを実装する
    - `agents/jp_stock_agent/app.py` に `BedrockAgentCoreApp` のエントリーポイントを実装する
    - 既存の `sample_agent/app.py` パターンを踏襲し、SSE ストリーミング対応の async generator を実装する
    - `callback_handler` でテキストチャンクを `asyncio.Queue` に流し、逐次 `yield` する
    - _要件: 8.1, 9.1_

- [x] 6. チェックポイント - エージェント全体の動作確認
  - すべてのテストが通ることを確認し、不明点があればユーザーに質問する。

- [x] 7. フロントエンド通信層とカスタムフックの実装
  - [x] 7.1 `useStockAnalysis` カスタムフックを実装する
    - `src/hooks/useStockAnalysis.ts` に株式分析専用のカスタムフックを実装する
    - 既存の `useAgentChat` パターンを参考に、`invokeRuntime` を使用して AgentCore Runtime と SSE 通信する
    - SSE ストリーム内の `<!--STOCK_DATA_JSON-->...<!--/STOCK_DATA_JSON-->` マーカーを検出し、JSON 部分をパースして `stockData` に格納する
    - マーカー外のテキストは `analysisText` に蓄積する
    - `analyze`, `analysisText`, `stockData`, `isAnalyzing`, `error` を返す
    - JSON パース失敗時はスキップしてテキスト分析のみ表示する
    - _要件: 8.1, 8.4, 10.3_

  - [ ]* 7.2 `useStockAnalysis` のユニットテストを作成する
    - SSE ストリーム内の JSON マーカー検出とパースのテスト
    - エラー状態の遷移テスト
    - _要件: 8.1, 10.3_

  - [ ]* 7.3 株価データ JSON ラウンドトリップのプロパティテストを作成する
    - **Property 9: 株価データ JSON ラウンドトリップ**
    - fast-check を使用し、ランダムな `StockDataPayload` に対して JSON シリアライズ→パースの等価性を検証する
    - **検証対象: 要件 10.4**

- [x] 8. フロントエンド UI コンポーネントの実装
  - [x] 8.1 `StockInputForm` コンポーネントを実装する
    - `src/components/stock/StockInputForm.tsx` に銘柄入力フォームを実装する
    - 単一のテキスト入力フィールドで銘柄名または4桁の銘柄コードを受け付ける
    - 空入力での送信を防止する
    - `disabled` プロパティで分析中の入力を制御する
    - _要件: 1.1_

  - [x] 8.2 `AnalysisReport` コンポーネントを実装する
    - `src/components/stock/AnalysisReport.tsx` に分析レポート表示コンポーネントを実装する
    - Markdown 形式の分析テキストを描画する
    - ストリーミング中はインクリメンタルに表示を更新する
    - 分析結果は投資助言ではなく参考情報である旨の免責事項を表示する
    - _要件: 7.5, 8.1, 8.2, 8.3_

  - [x] 8.3 `StockChart` コンポーネントを実装する
    - `src/components/stock/StockChart.tsx` に Recharts を使用した株価チャートコンポーネントを実装する
    - `package.json` に `recharts` を依存関係として追加する
    - 過去1年分の日次終値を折れ線グラフとして描画する
    - 5日、25日、75日、200日の移動平均線を色分けして重ねて表示する
    - 凡例を表示する
    - 出来高をチャート下部にバーチャートとして表示する
    - マウスホバー時に日付、終値、出来高、各移動平均値をツールチップで表示する
    - X軸に日付、Y軸に株価（円）を表示し、適切な目盛り間隔を自動設定する
    - _要件: 4.1, 4.2, 4.3, 4.4, 4.5, 4.6_

  - [ ]* 8.4 フロントエンドコンポーネントのユニットテストを作成する
    - `StockInputForm`: 空入力での送信防止テスト
    - `StockChart`: 有効なデータでのレンダリング確認テスト
    - `AnalysisReport`: 免責事項テキストの表示確認テスト
    - _要件: 1.1, 4.1, 7.5_

- [x] 9. ページ統合とストリーミング表示
  - [x] 9.1 `Stock_Analyzer_Page` を実装する
    - `src/app/stock-analyzer/page.tsx` に分析ページのルートコンポーネントを実装する
    - `StockInputForm`, `AnalysisReport`, `StockChart` を統合する
    - `useStockAnalysis` フックを使用してエージェントとの通信を管理する
    - 銘柄が特定された場合に企業名と銘柄コードを確認表示する
    - 分析中はローディングインジケーターを表示し、完了時に非表示にする
    - エラー発生時はエラーメッセージを表示し、再試行を促す
    - Runtime ARN 未設定時は設定案内メッセージを表示する（既存パターン踏襲）
    - _要件: 1.5, 8.1, 8.2, 8.3, 8.4_

  - [x] 9.2 ページスタイルとレイアウトを調整する
    - `src/app/stock-analyzer/` にページ用の CSS モジュールを作成する
    - レスポンシブ対応のレイアウトを実装する
    - _要件: 1.1, 4.1_

- [x] 10. 最終チェックポイント - 全体の動作確認
  - すべてのテストが通ることを確認し、不明点があればユーザーに質問する。

## 備考

- `*` マーク付きのタスクはオプションであり、MVP を優先する場合はスキップ可能
- 各タスクは具体的な要件番号を参照しており、トレーサビリティを確保している
- チェックポイントで段階的に動作を検証する
- プロパティテストは普遍的な正確性を検証し、ユニットテストは具体的なエッジケースを検証する
